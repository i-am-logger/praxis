# Citings — Ontology Diagnostics (Meta-Ontology)

Every published source this ontology stands on. This ontology's [README.md](README.md) uses an older structure and does not yet carry a dedicated *Key references* bullet list, so this file is a pointer to the sources embedded in the axiom doc comments and the workspace-wide bibliography.

## Primary sources

- See the axiom doc comments in `ontology.rs` for per-axiom attributions — each axiom typically names the source inline.
- Cross-reference the entries in [`docs/papers/references.md`](../../../../../../docs/papers/references.md) that cover this domain.

## Pending verification

This citings file was initialized as a stub by the per-ontology rollout (issue #57). Until a *Key references* section is added to the README and a full citations sweep is done, treat every source as pending:

- [ ] Add a *Key references* bullet list to `README.md`
- [ ] Extract per-axiom citations from `ontology.rs` doc comments into a *Primary sources* section here
- [ ] Cross-link each primary source to its full entry in `docs/papers/references.md`
- [ ] If a cited paper has no entry in the workspace bibliography, either add it there or place a local PDF under a `papers/` subdirectory

---

- **Document date:** 2026-04-14
- **Status:** stub — pending manual citation sweep
