fn main() {
    println!("praxis — axiomatic intelligence");
    println!();
    println!("The chatbot CLI requires ontologies not yet built:");
    println!("  1. Text ontology — what text IS (characters → tokens → sentences)");
    println!("  2. Text → Grammar functor — transforms text into syntax trees");
    println!("  3. Grammar → Semantics functor — extracts meaning from structure");
    println!("  4. Semantics → Pragmatics functor — identifies intent from meaning");
    println!("  5. Response generation — constructs output from ontology queries");
    println!();
    println!("Each step is a functor. Each preserves structure. Each is provable.");
    println!("No hardcoded string matching. No mechanical parsing. Only ontology.");
}
